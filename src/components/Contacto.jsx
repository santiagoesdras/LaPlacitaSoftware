export default function Contacto() {
  return (
    <section id="contacto" className="section section--muted" aria-labelledby="titulo-contacto">
      <div className="container flow">
        <header className="section-heading">
          <p className="eyebrow">Reservas y consultas</p>
          <h2 id="titulo-contacto">Contacto</h2>
          <p>
            Aquí se implementará el formulario accesible con validación de campos
            obligatorios y correo electrónico.
          </p>
        </header>

        <div className="placeholder">
          Formulario de contacto pendiente de implementación.
        </div>
      </div>
    </section>
  )
}
